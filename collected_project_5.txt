docs/CHANGELOG.md>>>
# CHANGELOG.md · SpeleoStart


## [Невыпущенное] — 02.05.2026

### 🔧 Исправлено
- **Title Case для русского языка:**
  - Заменён `Locale.getDefault()` на `Locale("ru", "RU")` в `StringExt.toTitleCase()`
  - Добавлена поддержка дефисных фамилий (Салтыков-Щедрин → Салтыков-Щедрин)
  - Корректная обработка букв `ё`, `й` на любом устройстве

### 📝 Документация
- Обновлены `ARCHITECTURE.md`, `VALIDATION_RULES.md`, `SPEC_MASTER.md` — явно указан запрет на `Locale.getDefault()` для русского языка


## [Невыпущенное] — 30.04.2026

### ✨ Добавлено
- `PersonDetailScreen`: экран карточки персоны с режимами просмотра/редактирования
- `TitleCaseTextField`: компонент с автозаглавной первой буквы и переходом по пробелу
- Маска даты рождения `ДД.ММ.ГГГГ` с автоматической расстановкой точек
- Цветная метка возраста (зелёный 18+, жёлтый 14-17, оранжевый <14 и >70)
- Чёрный список: добавление/удаление через `PersonDetailVM.blacklistPerson()`
- `PhoneFormatter`: маска телефона `+7 (XXX) XXX-XX-XX`

### 🔄 Изменено
- `DateValidator` переписан на `java.util.Calendar` (без `java.time` для совместимости с API 24)

### 🐛 Известные проблемы (НЕ РЕШЕНЫ)
- **Валидация даты:** поле не подсвечивается красным при незавершённом вводе (менее 10 символов)
- **Валидация даты:** `isError = true` только при длине ровно 10 символов
- **Автопереход:** не срабатывает при достижении 10 символов, если дата некорректна

### 🚫 NEVER-DO
- Не использовать `java.time.LocalDate` на API < 26 (краш `NoClassDefFoundError`)
- Не повышать версию БД без изменения полей `@Entity`
- Не давать инструкции частями — только полные файлы

---

## [1.0] — Прошлые версии (до 30.04.2026)

### ✨ Добавлено
- Базовая архитектура MVVM + Clean
- Room + Hilt + Compose BOM
- Экран создания соревнования (`CompetitionNewScreen`)
- Экран списка команд (`TeamListScreen`)
- Таймер с восстановлением через `app_settings`
- Финиш с фиксацией времени при открытии диалога
- Путевые листы с двойным подтверждением

### 🔄 Изменено
- Цветовые метки: эмодзи → текст (`18+`, `14+`, `<14ок`, `<17 !!!`, `<14 !!!`, `СНЯТЫ`)
- Метка «ДИСКВ» → «СНЯТЫ»

### 🐛 Исправлено
- Версия БД синхронизирована с изменениями Entity
- Порядок очистки БД: дети → родители
- `TestDataGenerator` обёрнут в `db.withTransaction`


## [Невыпущенное] — 01.05.2026

### ✨ Добавлено
- `PersonDetailScreen`: автопереход с поля даты рождения при достижении 10 символов (независимо от валидности)
- `PersonNewScreen`: визуальная подсветка ошибки в поле даты рождения (`isError` в `DateOfBirthField`)

### 🔄 Изменено
- `PersonDetailScreen`: `isFormValid` теперь блокирует сохранение при любой ошибке даты (`!dateHasError`)
- `PersonDetailVM.savePerson()`: добавлена жёсткая валидация даты (`length == 10` + `isRealDate()`), сохранение прерывается с ошибкой
- `PersonNewScreen`: валидация даты переключена с `isValid()` на `isRealDate()` (блокирует несуществующие даты: 31.02, 29.02.2023 и т.д.)
- `PersonNewScreen`: кнопка «СОХРАНИТЬ» блокируется при некорректной дате рождения
- `PersonDetailScreen`: поле даты рождения растягивается на всю ширину (`fillMaxWidth()`)

### 🗑️ Удалено
- `PersonNewScreen`: поле Email и весь связанный UI (`email`, `showEmail`, `AnimatedVisibility` блок Email)
- `PersonNewScreen`: дублирующее объявление `var showExtraFields`

### 🐛 Исправлено
- **Валидация даты:** поле теперь подсвечивается красным при ЛЮБОЙ ошибке (частичный ввод, несуществующая дата, выход за диапазон)
- **Валидация даты:** `isError = true` срабатывает на всех этапах ввода, не только при ровно 10 символах
- **Автопереход:** срабатывает при достижении 10 символов даже если дата некорректна (пользователь видит ошибку и может исправить)
- **Сохранение корявых дат:** полностью заблокировано на уровне UI (`isFormValid`) и VM (`savePerson`)
- **Дублирование переменной:** убран конфликт `showExtraFields` в `PersonNewScreen`

## [Невыпущенное] — 01.05.2026 04:34 МСК

### 🐛 Исправлено
- **TeamRegisterScreen**: поиск встроен в поле ввода фамилии (скрывался клавиатурой)
- **TeamRegisterScreen**: кнопка «➕ Создать персону» при пустом поиске
- **TeamRegisterScreen**: поле телефона — символы не писались (исправлен `TextFieldValue`)
- **TeamRegisterScreen**: поле даты рождения — скачки курсора (переписано через `BasicTextField` + `VisualTransformation`)
- **TeamRegisterScreen**: нельзя было выбрать ментора для участника <18 лет (исправлена логика `AgeGroup`)
- **TeamRegisterScreen**: добавлена кнопка «СОХРАНИТЬ ПЕРСОНУ» при быстром создании
- **TeamRegisterScreen**: быстрый ментор — ввод только фамилии и имени (без даты рождения)
- **TeamRegisterScreen**: поиск «Е» приравнен к «Ё» (нормализация строк)
- **TeamListScreen**: отображение ФИО участников в списке команд
- **TeamListVM**: загрузка участников и персон для отображения состава

### 🔄 Изменено
- `MemberDraft`: `val` → `var` для `quick*` полей (исправление `Val cannot be reassigned`)
- `TeamRegisterVM`: добавлен `createQuickPerson()` для сохранения быстрой персоны
- `PersonDao.searchPersons()`: добавлена нормализация `Ё→Е` в запросе

## [Невыпущенное] — 01.05.2026 05:55 МСК

### 🐛 Исправлено
- **TeamRegisterScreen: поиск скрыт клавиатурой** — поисковый блок (`Card` с фоном `primaryContainer`) поднят выше по иерархии, перед списком участников. Клавиатура больше не перекрывает результаты поиска.

- **TeamRegisterScreen: роль «Капитан» занимает слишком много места** — сделан компактный стиль: `fontSize = 11.sp`, `padding(horizontal = 8.dp, vertical = 2.dp)`, фон `primaryContainer`. Теперь роль отображается узкой полоской.

- **TeamRegisterScreen: дата рождения «скачет» курсор** — полностью переписано по образцу `PersonNewScreen`:
    - `var dateState by remember { mutableStateOf(TextFieldValue(...)) }`
    - синхронизация через `LaunchedEffect`
    - курсор стабилен, нет самопроизвольных перемещений

- **TeamRegisterScreen: телефон не пишется (символы не отображаются)** — исправлено по образцу `PersonNewScreen`:
    - `var phoneState by remember { mutableStateOf(TextFieldValue(...)) }`
    - фильтрация цифр + `PhoneFormatter.formatAsYouType()`
    - теперь символы вводятся корректно

- **TeamRegisterScreen: пустая выдача поиска по-старому** — кнопка создания заменена с `TextButton` на полноценную `Button` с текстом «➕ Создать: \$searchQuery». Видна сразу при пустом результате поиска, без дополнительных кликов.

### 🔄 Изменено
- `TeamRegisterScreen`: структура композиции переработана — поиск вынесен в отдельный верхний блок, список участников ниже

### ✅ Статус
- Все известные проблемы ввода телефона и даты рождения в `TeamRegisterScreen` устранены
- Поиск и создание персон теперь работают предсказуемо при любой высоте клавиатуры

## [Невыпущенное] — 01.05.2026 06:30 МСК
### 🐛 Исправлено
- **TeamRegisterScreen**: курсор даты рождения стабилен (синхронизация `TextFieldValue` + `LaunchedEffect`)
- **TeamRegisterScreen**: созданная «быстрая» персона (<18) автоматически открывает выбор ментора
- **TeamRegisterScreen**: диалог «Участник занят» → кнопка «Перейти» корректно навигирует в карточку команды
- **TeamRegisterVM**: `UiEvent.ConflictDialog` теперь передаёт `teamId` для навигации

### 🔄 Изменено
- `TeamRegisterScreen`: добавлен параметр `onNavigateToTeam: (Long) -> Unit`
- `TeamRegisterVM.UiEvent.ConflictDialog`: добавлено поле `teamId: Long`
- Логика валидации: при создании участника <18 сразу предлагается выбрать/создать ментора

### ✅ Статус
- Все 4 пункта ТЗ по экрану «Регистрация» реализованы и протестированы

## [2026-05-01] — Экран настроек соревнования

### Добавлено
- Полная реализация `CompetitionSettingsScreen` по ТЗ
- `CompetitionSettingsVM` с загрузкой по `competitionId`, валидацией, сохранением `settingsJson`
- Отображение КП кластерами по классам с цветовой дифференциацией (normal/technical)
- Диалог редактирования типа КП
- Интеграция с `MasterRouteCardRepository` через Flow
- Визуальная группировка КП в рамки с label класса на границе

### Изменено
- `Icons.Default.ArrowBack` → `Icons.AutoMirrored.Filled.ArrowBack` (deprecated fix)

### Технические детали
- Sealed class `SettingsUiEvent` (изолирован от общего `UiEvent`)
- Использование `FlowRow` для адаптивного расположения кластеров
- `OutlinedCard` + круглый label с `offset` для позиционирования на границе

## [2026-05-01] — Экран настроек соревнования

### Добавлено
- Полная реализация `CompetitionSettingsScreen` по ТЗ
- `CompetitionSettingsVM` с загрузкой по `competitionId`, валидацией, сохранением `settingsJson`
- Отображение КП кластерами по классам (КЛАСС 2 / КЛАСС 3)
- Цветовая дифференциация КП: зелёный `#2E7D32` (normal), оранжевый `#F57C00` (technical)
- Диалог редактирования типа КП (обычный/технический)
- Интеграция с `MasterRouteCardRepository` через Flow
- Визуальная группировка КП в рамки `OutlinedCard` с круглым label класса на границе
- Поле «Минимальный состав команды» в `settingsJson`

### Изменено
- `Icons.Default.ArrowBack` → `Icons.AutoMirrored.Filled.ArrowBack` (deprecated fix)
<<<конец файла

docs/CURRENT_STATE.md>>>
# ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА (SNAPSHOT)
**Дата:** 02.05.2026  
**Статус:** Рабочий билд, компилируется

---

## 1. ЧТО РАБОТАЕТ КОРРЕКТНО

### TeamListScreen
- Поиск по номеру команды ✅
- Поиск по фамилии участника (полное совпадение) ✅
- Поиск по частичному совпадению (через memberFullInfo) ✅
- Поиск по нику (позывному) ✅
- Крестик очистки поиска ✅
- Отображение статуса команды с цветом ✅
- Отображение цветовой метки (18+, 14+, <14ок, <17!!!, <14!!!, СНЯТЫ) ✅

### TeamCardScreen
- Загрузка и отображение карточки команды ✅
- Отображение состава (ФИО, возраст, телефон, ник) ✅
- Отображение ментора (если назначен) ✅
- Отображение статуса путевого листа ✅
- Ссылка на заполнение ПЛ (для finished без подтверждённого ПЛ) ✅
- Расформирование registered команды (без пароля) ✅
- Расформирование started команды (пароль 1234/devdebug) ✅
- Исключение участника (статус free_agent) ✅
- Замена участника через поиск (если персона найдена) ✅
- История замен (разворачивается, пока заглушка) ✅

### StringExt / Title Case
- Нормализация ФИО перед сохранением в БД ✅
- Extension функция normalizeName() ✅
- Импорты добавлены во все файлы ✅

---

## 2. ЧТО НЕ РАБОТАЕТ / НЕ ДОДЕЛАНО

### Баги
1. **Title Case при быстром создании персоны через замену** — не применяется (персона создаётся как введено, без нормализации)

### Требует доработки
1. **Диалог выбора ментора** — кнопка "⚠️ Требуется ментор" присутствует, но действие не реализовано
2. **Список заменённых участников** — вместо реальных данных заглушка
3. **Причина расформирования** — выводится в сообщении, но не сохраняется в БД
4. **Пароль судьи** — хардкод 1234, нужно вынести в настройки

---

## 3. СЦЕНАРИИ ДЛЯ ПРОВЕРКИ

### Замена участника
1. Открыть карточку команды
2. Кликнуть по участнику → кнопка "Заменить"
3. В поиске ввести фамилию существующей персоны → выбрать → замена работает
4. В поиске ввести новую фамилию → нажать "➕ Создать: фамилия"
5. **ОЖИДАЕТСЯ:** персона создаётся с нормализацией Title Case (первая буква заглавная, остальные строчные)
6. **ФАКТ:** персона сохраняется как введено (без нормализации)

---

## 4. СЛЕДУЮЩИЕ ШАГИ

1. Исправить Title Case при быстром создании персоны в TeamCardVM.createQuickPerson()
2. Реализовать диалог выбора ментора
3. Загружать реальные данные в историю замен
4. Вынести пароль судьи в настройки
<<<конец файла

docs/Logic_Processes.md>>>
# ЛОГИКА ПРОЦЕССОВ · СпелеоСтарт

---

## 1. РЕГИСТРАЦИЯ КОМАНДЫ

```text
НАЧАЛО
│
▼
Ввод фамилии (3+ символов)
│
├──[источник "База персон"]──▶ Поиск в persons
│   ├──[найдено]──▶ Выпадающий список → выбор → автозаполнение
│   └──[не найдено]──▶ Ручной ввод
│
└──[источник "Словарь"]──▶ Поиск в names_dictionary.json
    ├──[найдено]──▶ Список фамилий → выбор → автоподстановка имени
    └──[не найдено]──▶ Ручной ввод
```

### Проверка возраста и ментора
```text
Введена дата рождения
│
▼
Расчёт возраста
│
├──[≥18]──▶ ✅ Ментор не требуется
├──[14-17]──▶ ⚠️ Требуется ментор (подтверждение)
├──[<14]──▶ ⚠️ Ментор + разрешение судьи
└──[некорректна]──▶ ❌ Ошибка
```

### Сохранение
```text
Нажата "СОХРАНИТЬ"
│
▼
Проверки:
├──[командный]──▶ мин. 2 участника
├──[личный]─────▶ мин. 1 участник
├──[ФИО заполнены?]
└──[обязательные поля?]
│
▼
✅ Создание: номер (сквозной в классе), статус registered, цветовая метка
```

---
## 2. СТАРТ

### Формирование очереди
```text
Получить все registered команды
│
▼
Разделить по классам: class2_list, class3_list
│
▼
Сортировать по team_number
│
▼
Чередование: 2 → 3 → 2 → 3 ...
```

### Старт команды
```text
Нажата "СТАРТ" или обратный отсчёт = 0
│
▼
Проверки:
├──[команда существует?]
├──[статус = registered?]
├──[мин. участников?]
└──[менторы для <18?]
│
▼
✅ Статус → started
start_time = System.currentTimeMillis()
Удалить из очереди
countdown = start_interval
```

### Пропуск
```text
Нажата "ПРОПУСК"
│
├──[первая команда?]──▶ ❌ Нельзя
└──[команда существует?]
│
▼
✅ skip_count++
Перенести в конец очереди своего класса
countdown = start_interval
```

### Откат старта
```textДолгий тап → "Откат старта"
│
├──[единственная started?]──▶ Сброс таймера, статус → registered, кнопка "НАЧАТЬ"
└──[есть другие started?]──▶ Статус → registered, возврат в конец очереди
```

---

## 3. ФИНИШ
```text
Открыт диалог финиша (FAB 🏁)
│
▼
finishTimestamp = System.currentTimeMillis()  ← ФИКСАЦИЯ СРАЗУ
│
▼
Показать все started команды (2 колонки)
│
▼
Выбор команд → "ПОДТВЕРДИТЬ"
│
▼
Для каждой выбранной:
finishTimestamp = зафиксированное время
Статус → finished
```

### Ручная корректировка
```text
Долгий тап по времени
│
▼
Диалог: часы, минуты, секунды
│
▼
Лог: "Время скорректировано. Судья: [ФИО]. Исходное: X, Новое: Y"
```

---

## 4. ПУТЕВОЙ ЛИСТ

### Заполнение КП
```text
Для каждого КП:
│
├──[обычный]──▶ Чекбоксы "Взят" / "Ошибка"
│   ├──[☑️ Взят, ☐ Ошибка]──▶ Вес засчитывается
│   ├──[☑️ Взят, ☑️ Ошибка]──▶ Вес НЕ засчитывается
│   └──[☐ Взят]─────────────▶ Вес НЕ засчитывается│
└──[технический]──▶ Дополнительно: Отсечка (ММ:СС), Штраф (баллы)
```

### Подтверждение
```text
Секретарь заполнил → пароль → "☑️ Карту заполнил"
Судья проверил → пароль → "☑️ Карту проверил"
│
▼
Оба отмечены → checkpointsEntered = true → read-only
```

### Пересчёт lost → finished
```text
После сохранения ПЛ:
│
▼
чистое_время = (финиш - старт) - Σ(отсечка)
│
▼
Если статус = lost И чистое_время ≤ КВ:
│
▼
✅ Статус → finished
Лог: "Статус изменён с lost на finished после учёта ожидания"
```

---

## 5. ИТОГИ

### Расчёт баллов
`Σ(баллы) = Σ(вес_КП, где taken=1 И error=0) - Σ(штрафы)`

### Ранжирование
```text
Сортировка:
Статус: finished > lost > disqualified
Баллы: больше → выше
Чистое время: меньше → выше
```

### Деление мест
```text
Если баллы равны И разница времени ≤ допуск:
│
▼
Предложить судье объединить
│▼
final_place = одинаковый
place_merged = true
```

---

## 6. РАСФОРМИРОВАНИЕ
```text
Долгий тап → "Расформировать"
│
├──[registered]──▶ Без пароля → участники → free_agent
├──[started]─────▶ Пароль + причина → возврат в registered → free_agent
└──[finished/lost/disqualified]──▶ ❌ Запрещено
```

---

## 7. СМЕНА КЛАССА
```text
│
├──[registered]──▶ Можно. Если в очереди → выбор: оставить/в конец/удалить
├──[started ≤5 мин]──▶ Пароль + штраф (настраивается)
├──[started >5 мин]──▶ ❌ Только откат или дисквалификация
└──[finished]────────▶ ❌ Только дисквалификация
```

---

## 8. ПЕРЕХОДЫ СТАТУСОВ КОМАНДЫ
```text
┌─────────────┐     СТАРТ      ┌─────────┐    ФИНИШ     ┌──────────┐
│ registered  │───────────────▶│ started │─────────────▶│ finished │
└──────┬──────┘                └────┬────┘              └────┬─────┘
       │                            │                         │
       │ ДИСКВАЛИФИКАЦИЯ             │ ДИСКВАЛИФИКАЦИЯ          │ ДИСКВАЛИФИКАЦИЯ
       ▼                            ▼                         ▼
┌─────────────┐                ┌─────────────┐           ┌─────────────┐
│disqualified │                │disqualified │           │disqualified │
└─────────────┘                └─────────────┘           └─────────────┘
       │
       │ ПРОСРОЧКА КВ
       ▼
┌─────────┐
│  lost   │
└────┬────┘
     │ УЧЁТ ОЖИДАНИЯ (чистое_время ≤ КВ)
     ▼
┌──────────┐
│ finished │└──────────┘
```

---

## 9. ПРОВЕРКИ И ОГРАНИЧЕНИЯ (СВОДКА)

| Действие | Условие | Результат |
|----------|---------|-----------|
| Старт | ≥ мин. участников | ❌ «Недостаточно участников» |
| Старт | Все участники active | ❌ «Есть неактивные» |
| Пропуск | Не первая команда | ❌ «Нельзя пропустить первую» |
| Расформирование | registered или started | ❌ «Нельзя расформировать» |
| Финиш | Статус started | ❌ «Не на дистанции» |
| Подтверждение ПЛ | Не взаимоисключающие | ❌ «Нельзя отметить оба» |
| Финализация | Все finished имеют ПЛ | ❌ «Есть незавершённые» |
| Сброс таймера | Нет started команд | ❌ «Есть команды на дистанции» |
<<<конец файла

docs/README.md>>>
# СпелеоСтарт (SpeleoStart)

Приложение для судейства соревнований по подземному ориентированию.
Работает полностью офлайн в пещерах и каменоломнях.

## Быстрый старт

1. Установите APK на планшет/телефон (Android 7.0+)
2. Запустите приложение
3. Создайте соревнование (**Соревнования** → **➕ Новое**)
4. Добавьте персон (**Персоны** → **➕ Новая**)
5. Зарегистрируйте команды (**Регистрация**)
6. Начинайте старты (**Старт** → **НАЧАТЬ СОРЕВНОВАНИЯ**)

## Основные функции

- Регистрация команд и участников (личный/командный зачёт)
- Автоматическое чередование классов на старте (`2→3→2→3`)
- Таймер с восстановлением после краша/перезагрузки
- Фиксация финиша с ручной корректировкой времени
- Электронные путевые листы (ПЛ) с двойным подтверждением
- Автоматический подсчёт баллов и итогов
- Экспорт протоколов (JSON/CSV) и генерация грамот (PDF)
- Поддержка произвольного состава команды (ОБГ)

## Технический стек

- **Kotlin** 1.9.22 + **Jetpack Compose** (BOM 2024.02.00)
- **Room** 2.6.1 (SQLite)
- **Hilt** 2.48 (DI)
- **Navigation Compose** 2.7.7
- **Coroutines + Flow** (асинхронность)
- **Timber** 5.0.1, **Security Crypto** 1.1.0

## Структура проекта

```text
app/src/main/java/com/speleo/start/
├── data/
│   ├── local/
│   │   ├── dao/        # Интерфейсы доступа к БД
│   │   └── entity/     # Таблицы БД
│   └── repository/     # Репозитории
├── di/                 # Модули Hilt
├── domain/
│   └── usecase/        # Бизнес-логика
├── presentation/
│   └── screen/         # Экраны (Compose)
└── util/               # Утилиты (даты, телефоны, таймеры)
```

## Разработка

### Отладка (Debug-режим)
- Долгий тап по плитке **«Настройки»** → меню разработчика
- **«Заполнить тестовыми»** — создаёт соревнование, 20 персон, 10 команд
- **«Очистить тестовые»** — удаляет все данные (соблюдая порядок `дети → родители`)
- Таймер в debug-режиме работает ускоренно (`2 сек = 1 сек реального времени`)

### Пароли по умолчанию (Debug)
| Роль | Пароль |
|------|--------|
| Мастер | `devdebug` |
| Судья | `judge123` |
| Секретарь | `secret123` |
| Администратор | `1234` |

## Важные правила разработки

1. При изменении `Entity` **ВСЕГДА** повышайте `version` в `AppDatabase`.
2. Порядок очистки БД: дочерние таблицы → родительские (`participants` перед `teams`).
3. Не хардкодить `competitionId`/`teamId` — используйте `SharedState`/ViewModel.
4. Всегда проверяйте результат `insert()` на `-1L`.

## Лицензия
Проприетарная. Все права защищены.

## Контакты
- **Разработка:** команда SpeleoStart
- **Документация:** `TZ_v4_Full.md`, `Logic_Processes.md`, `TZ_Corrections.md`

## [Невыпущенное] — 01.05.2026 18:23 МСК

### 🐛 Исправлено
- **TeamRegisterScreen: курсор даты рождения прыгает перед символом** — восстановлен пересчёт `digitsBeforeCursor + dotsBeforeCursor` после регрессии
- **TeamRegisterScreen: ментор не прикрепляется / нет кнопки изменить** — `ageGroup` теперь обновляется при сохранении быстрой персоны (`calculateAgeGroup(it.birthDate)`)
- **TeamRegisterScreen: кнопка «СОХРАНИТЬ КОМАНДУ» заблокирована после быстрого создания** — `AgeGroup.UNKNOWN` больше не остаётся в `MemberDraft` после `createQuickPerson`

### 🛡️ Правила (добавлены в ARCHITECTURE.md / VALIDATION_RULES.md / SPEC_MASTER.md)
- **Курсор автоформатирования:** запрещён `selection.start.coerceIn()` напрямую, обязателен пересчёт через значащие символы
- **ageGroup при quick-create:** обязательно вычислять и сохранять в `MemberDraft` при `createQuickPerson()`, иначе `allRequirementsMet` возвращает `false` для `UNKNOWN`
- **effectiveAgeGroup в MemberCard:** резервный расчёт на лету если `ageGroup == UNKNOWN` (защита от legacy-данных)
<<<конец файла

docs/SPEC_MASTER.md>>>
# 📘 SPEC_MASTER.md · SpeleoStart v4.1 (Single Source of Truth)
**Дата:** 26.04.2026 | **Статус:** Рабочая версия | **Язык:** RU

## 1. ГЛОССАРИЙ (ОПЕРАЦИОННЫЙ)
| Термин | Значение |
|--------|----------|
| `Competition` | Мероприятие. Цикл: `СОЗДАНО → АКТИВНО → ЗАВЕРШЕНО → АРХИВ → УДАЛЕНО` |
| `Team` | Команда. Статусы: `registered → started → finished / lost / disqualified` |
| `Checkpoint (КП)` | Точка. Типы: `normal` (вес), `technical` (вес + отсечка + штраф) |
| `Master Route Card` | Эталонный набор КП соревнования |
| `Team Route Card (ПЛ)` | Фактические отметки команды. Двойное подтверждение: Секретарь → Судья |
| `КВ` | Контрольное время. 2 кл = 90 мин, 3 кл = 60 мин |
| `Ментор` | Лицо ≥18 лет. Подтверждает ответственность за участника <18 |
| `ПСР` | Статус `lost`. Требует флага `psrResolved` для закрытия |

## 2. ЦВЕТОВЫЕ МЕТКИ КОМАНД
| Метка | Условие | Цвет |
|-------|---------|------|
| `18+` | Все ≥18 | `#00A86B` |
| `14+` | Есть 14-17, `mentorConfirmed = true` | `#FFC107` |
| `<14ок` | Есть <14, ментор + `judgeApproved = true` | `#FFC107` |
| `<17 !!!` | Есть 14-17, ментор НЕ подтверждён | `#FF8C00` |
| `<14 !!!` | Есть <14, НЕТ разрешения судьи | `#FF8C00` |
| `СНЯТЫ` | `disqualified` | `#D32F2F` |

## 3. КЛЮЧЕВЫЕ ПРОЦЕССЫ
### 3.1 Старт
- Очередь чередуется: `2 → 3 → 2 → 3`. Если класс пуст → оставшиеся подряд.
- Первый старт: обратный отсчёт 10 сек. Далее: `start_interval` из настроек.
- `finishTimestamp` фиксируется **при открытии диалога финиша**, не при подтверждении.
- Время главного таймера: `System.currentTimeMillis() - startTimestamp`. Восстанавливается из `app_settings`.

### 3.2 Финиш
- Групповой финиш разрешён. Всем выбранным командам присваивается одинаковый `finishTimestamp`.
- Правка времени: долгий тап → диалог `ЧЧ:ММ:СС` → запись в лог: `"Время скорректировано. Судья: [ФИО]. Исходное: X, Новое: Y"`.

### 3.3 Путевой лист
- `Взят + Ошибка` → вес = 0, отсечка учитывается.
- `Не взят` → вес = 0, отсечка не учитывается.
- `Чистое время = (финиш - старт) - Σ(отсечка)`. Если `≤ КВ` и статус `lost` → автоперевод в `finished`.
- После подтверждения Секретарем + Судьёй → `checkpointsEntered = true` → **read-only**.

### 3.4 Итоги
- Сортировка: `Статус (finished>lost>disq) → Баллы (↑) → Чистое время (↓)`.
- Объединение мест: если баллы равны и разница времени `≤ tolerance` (по умолч. 2 сек) → судья подтверждает `place_merged = true`.

## 4. ТЕХНИЧЕСКИЕ ОГРАНИЧЕНИЯ
- **БД:** Room 2.6+, версия `5`. `version++` при любом изменении `Entity`.
- **ID:** Никогда не хардкодить `competitionId` / `teamId`. Передача через `SharedState`.
- **Очистка БД:** Порядок `дети → родители`. `clearAll()` в `TestDataGenerator` обёрнут в `db.withTransaction`.
- **Безопасность:** Пароли SHA-256 + соль. Debug-режим: `devdebug`, `judge123`, `secret123`, `1234`.
- **UI:** Тёмная тема адаптивная. Хардкод цветов запрещён (`MaterialTheme.colorScheme` или проверка на обе темы). Мин. тап 48dp.
- **Курсор в автоформатировании:** Запрещено использовать `newVal.selection.start` как новую позицию курсора после вставки разделителей. Обязателен пересчёт через количество значащих символов. Нарушение → регрессия «прыгающего курсора».

## 5. NEVER-DO (КРАТКО)
1. Не повышать версию БД при изменении Entity → **КРАШ**.
2. Хардкодить ID → **НЕСОГЛАСОВАННОСТЬ**.
3. Вызывать генератор тестовых данных при старте → **ДУБЛИКАТЫ**.
4. Удалять родителей до детей → **FOREIGN KEY**.
5. Растягивать правки одного файла на итерации → **КОНФЛИКТЫ**.
<<<конец файла

docs/TZ_Corrections.md>>>
# ИСТОРИЯ ПРАВОК И КОРРЕКТИРОВОК ТЗ
*с пояснениями «почему именно так»*

1. **Добавлено поле `shortName` в `CompetitionEntity`**
   - **Дата:** 24.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** Полное название «Бяковские каменоломни 2026» слишком длинное для статус-бара (48dp высоты). Экономия места на экране. В протоколах используется полное `name`.
   - **Файлы:** `CompetitionEntity.kt`, `CompetitionNewScreen.kt`, `CompetitionNewVM.kt`

2. **Добавлено поле `nickname` (позывной) в `PersonEntity`**
   - **Дата:** 24.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** В спелеосообществе принято использовать позывные. Полезно для быстрой идентификации.
   - **Файлы:** `PersonEntity.kt`, `PersonNewScreen.kt`

3. **Цветовые метки команд заменены с эмодзи 🟢🟠🔴 на текст**
   - **Дата:** 24.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** Эмодзи по-разному отображаются на разных устройствах. Текст читается однозначно.
   - **Было:** `🟢 Зелёная метка`
   - **Стало:** `18+`, `14+`, `<14ок`, `<17 !!!`, `<14 !!!`, `СНЯТЫ`

4. **Метка «ДИСКВ» заменена на «СНЯТЫ»**
   - **Дата:** 24.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** «СНЯТЫ» — более понятный русский термин, чем сокращение «ДИСКВ».

5. **«⚠️ Разрешено судьёй» — цвет изменён с красного на тёмно-зелёный**
   - **Дата:** 24.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** Красный цвет ассоциируется с ошибкой. Разрешение — это наоборот «всё в порядке». Тёмно-зелёный `#2E7D32`.

6. **Аватары: свои SVG вместо эмодзи 👤👦👧**
   - **Дата:** 24.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** Эмодзи выглядят по-разному на Android и iOS. SVG гарантирует единый стиль.
   - **Файлы:** `ic_avatar_male.svg` (голубой), `ic_avatar_female.svg` (розовый), `ic_avatar_default.svg` (серый)

7. **Таймер: формат `ММ:СС.сс`, часы мельче если `00`**
   - **Дата:** 25.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** Экономия места, пока соревнование не перевалило за час. Сотые важны для финиша.

8. **Время финиша фиксируется при открытии диалога, а не при подтверждении**
   - **Дата:** 23.04.2026
   - **Инициатор:** Разработчик (ты) + обсуждение
   - **Причина:** Пока судья выбирает команды в списке, время не должно «тикать». Это справедливо для группового финиша.

9. **Отсечка вычитается из времени, даже если КП «взят с ошибкой»**
   - **Дата:** 22.04.2026
   - **Инициатор:** Разработчик (ты)
   - **Причина:** Команда всё равно ждала в очереди и потратила время. Справедливо.

10. **Смена класса разрешена первые 5 минут после старта со штрафом**
    - **Дата:** 22.04.2026
    - **Инициатор:** Разработчик (ты)
    - **Причина:** Реальный кейс: команда поняла, что ошиблись классом, но ещё не ушла далеко. Штраф настраивается в настройках.

11. **Кнопка «Экспорт сейчас» на главном экране**
    - **Дата:** 24.04.2026
    - **Инициатор:** Разработчик (ты)
    - **Причина:** Страх судей потерять данные. Быстрый доступ без перехода в настройки.

12. **Автоэкспорт в фоне при каждом изменении**
    - **Дата:** 24.04.2026
    - **Инициатор:** Обсуждение
    - **Причина:** Если приложение крашнется — данные не потеряются. Файл `autobackup.json` ~300 КБ.

13. **Импорт в «голое» приложение**
    - **Дата:** 24.04.2026
    - **Инициатор:** Разработчик (ты)
    - **Причина:** Сценарий: телефон сломался, флешка → новый телефон → восстановление.

14. **Таймер восстанавливается через БД, а не SharedPreferences**
    - **Дата:** 25.04.2026
    - **Инициатор:** Обсуждение (вариант Б)
    - **Причина:** SharedPreferences может сброситься при очистке кэша. БД надёжнее.

15. **Порядок очистки БД: дети → родители**
    - **Дата:** 25.04.2026
    - **Инициатор:** Сторонний наблюдатель
    - **Причина:** `FOREIGN KEY constraint` при удалении родительской таблицы раньше дочерней.

16. **Генератор тестовых данных обёрнут в `db.withTransaction`**
    - **Дата:** 25.04.2026
    - **Инициатор:** Сторонний наблюдатель
    - **Причина:** Атомарность: если генерация упадёт на середине — не останется «мусора» в БД.

17. **Версия БД повышается при ЛЮБОМ изменении Entity**
    - **Дата:** 25.04.2026 (выстрадано)
    - **Инициатор:** 4-часовой баг `FOREIGN KEY`
    - **Причина:** Старые данные + новая схема = крах. Всегда `version++`.
<<<конец файла

docs/TZ_v4_Full.md>>>
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ v4.1
## Подземное ориентирование · СпелеоСтарт

---

## РАЗДЕЛ 1: ГЛОССАРИЙ (18 терминов)

### 1.1. Соревнование (Competition)
Отдельное мероприятие по спортивному ориентированию.

**Свойства:**
- `name` — полное название (для протоколов)
- `shortName` — краткое название (для статус-бара)
- `date`, `place`, `discipline`, `system`, `settingsJson`
- Жизненный цикл: СОЗДАНО → АКТИВНО → ЗАВЕРШЕНО → АРХИВИРОВАНО → УДАЛЕНО (и ОТМЕНЕНО)

### 1.2. Персона (Person)
Физическое лицо в общей базе. ФИО, дата рождения, пол, телефон, email, заметка, чёрная метка, никнейм, `canBeMentor`.

### 1.3. Участник (Participant)
Роль персоны в команде. Статусы: `active`, `replaced`, `free_agent`. Роли: `captain`, `member`.

### 1.4. Свободный агент (Free Agent)
Участник без активной команды. Может быть добавлен в другую.

### 1.5. Команда (Team)
Группа участников (от 2). Статусы: `registered`, `started`, `finished`, `disqualified`, `lost`.
Цветовые метки: `18+`, `14+`, `<14ок`, `<17 !!!`, `<14 !!!`, `СНЯТЫ`.

### 1.6. Класс сложности (Class)
2-й класс (КВ 90 мин), 3-й класс (КВ 60 мин). Разные наборы КП.

### 1.7. КП (Checkpoint)
Точка на дистанции. Номер, вес, тип (обычный/технический), норматив, бонус.

### 1.8. Эталонный маршрутный лист (Master Route Card)
Конфигурация всех КП из настроек.

### 1.9. Путевой лист (Team Route Card)
Фактические отметки команды по КП. Подтверждается секретарём и судьёй.

### 1.10. Старт (Start)
Момент начала движения. Интервал из настроек, чередование 2→3→2→3, первая команда 10 сек.

### 1.11. Финиш (Finish)
Время финиша = момент открытия диалога. Множественный финиш. Ручная корректировка.

### 1.12. Главный таймер (Main Timer)
`ММ:СС.сс`, от первого старта. Не останавливается. Восстанавливается после краша через `app_settings`.
### 1.13. Обратный отсчёт (Countdown Timer)
До старта текущей команды. Звук 800/1000 Гц.

### 1.14. КВ (Control Time)
90 мин (2-й класс), 60 мин (3-й класс). Просрочка → `lost`.

### 1.15. ПСР (Search & Rescue)
Статус `lost`, флаг `psrResolved`.

### 1.16. Откат старта (Rollback)
Возврат в `registered`. Если единственная на дистанции → сброс таймера.

### 1.17. Настройки соревнования (Competition Settings)
JSON с параметрами: интервал, КВ, КП, кастомные поля.

### 1.18. Ментор (Mentor)
Совершеннолетний (≥18), ответственный за участника <18. Может быть участником другой команды.

---

## РАЗДЕЛ 2: СУЩНОСТИ И СВЯЗИ

### Основные связи
| Связь | Тип | Внешний ключ |
|-------|-----|-------------|
| COMPETITION → TEAM | 1:N | `team.competitionId` |
| TEAM → PARTICIPANT | 1:N | `participant.teamId` |
| PERSON → PARTICIPANT | 1:N | `participant.personId` |
| PERSON → MENTOR | 1:1 | `mentor.personId` |
| MENTOR → PARTICIPANT | 1:N | `participant.mentorId` |
| COMPETITION → MASTER_ROUTE_CARD | 1:N | `master_route_card.competitionId` |
| MASTER_ROUTE_CARD → TEAM_ROUTE_CARD | 1:N | `team_route_card.checkpointId` |

### Правила целостности
- **R1:** Не стартовать без мин. участников
- **R2:** Одна персона не дважды в одной команде
- **R3:** Персона активна только в одной команде соревнования
- **R4:** Ментор ≥18 лет
- **R5:** Не редактировать подтверждённый ПЛ

---

## РАЗДЕЛ 3: БАЗА ДАННЫХ

### Таблицы
1. `competitions` — соревнования
2. `persons` — персоны
3. `teams` — команды
4. `participants` — участники
5. `mentors` — менторы6. `master_route_card` — эталонный МЛ
7. `team_route_card` — путевые листы
8. `app_settings` — настройки (key-value)

### Индексы
Созданы для всех внешних ключей и полей поиска.

### Триггеры
- `check_person_active_in_one_team` — R3
- `check_mentor_age` — R4
- `prevent_edit_confirmed_route_card` — R5

### Версия БД: 5

---

## РАЗДЕЛ 4: СОРЕВНОВАНИЯ

### Жизненный цикл
`СОЗДАНО → АКТИВНО → ЗАВЕРШЕНО → АРХИВИРОВАНО → УДАЛЕНО`  
`↓`  
`ОТМЕНЕНО`

### Создание (мастер)
1. Основное: `name`, `shortName`, `date`, `place`, `discipline`, `system`
2. Параметры старта: интервал, КВ
3. Классы: 2-й (90 мин), 3-й (60 мин)
4. Возраст: мин. 14 лет, правило <14, формат даты
5. КП: список с весами и типами
6. Пользовательские поля

### Финализация
Требует: все ПЛ введены, все статусы финальные. Пароль судьи. Данные → `read-only`.

---

## РАЗДЕЛ 5: УЧАСТНИКИ И ПЕРСОНЫ

### Персона
- ФИО (`lastName`, `firstName`, `middleName`)
- `nickname` (позывной)
- `birthDate`, `phone`, `email`, `gender`
- `note` (заметка), `blacklisted` (чёрная метка)
- `canBeMentor` (флаг «может быть ментором», ≥18 лет)

### Участник
- Статусы: `active`, `replaced`, `free_agent`
- Роль: `captain`, `member`
- `mentorId`, `mentorConfirmed`, `judgeApproved`
### Ментор
- ≥18 лет, может быть участником другой команды
- Один ментор — много подопечных
- Подтверждение ответственности

### Автоподстановка
- Поиск по 3+ символам фамилии или 4+ цифрам телефона
- Источник: база персон или словарь имён

---

## РАЗДЕЛ 6: КОМАНДЫ

### Статусы
`registered` → `started` → `finished`  
Любой → `disqualified`  
`started` → `lost` → (учёт ожидания) → `finished`

### Цветовые метки
- `18+` — все ≥18
- `14+` — есть 14-17, ментор подтверждён
- `<14ок` — есть <14, ментор + разрешение
- `<17 !!!` — есть 14-17, ментор НЕ подтверждён
- `<14 !!!` — есть <14, НЕТ разрешения
- `СНЯТЫ` — `disqualified`

### Нумерация
Сквозная в рамках класса и соревнования.

### Расформирование
- `registered` → без пароля, участники → `free_agent`
- `started` → с паролем + причина + возврат в `registered`
- `finished/lost/disqualified` → запрещено

---

## РАЗДЕЛ 7: СТАРТ

### Очередь
- Из `registered` команд
- Чередование 2→3→2→3
- Если класс пуст — подряд оставшиеся
- Пустая очередь → отсчёт остановлен, ждёт

### Таймеры
- Главный: `System.currentTimeMillis() - startTimestamp`
- Обратный: 10 сек (первая), интервал из настроек (остальные)
- Восстановление через `app_settings`

### Кнопки- НАЧАТЬ СОРЕВНОВАНИЯ → 10-сек отсчёт
- СНЯТЬ, ПРОПУСК, СТАРТ
- ПАУЗА/ПУСК

### Drag-and-drop очереди
Запрещён при ≤10 сек до старта.

---

## РАЗДЕЛ 8: ФИНИШ

### Фиксация времени
- `finishTimestamp = System.currentTimeMillis()` при открытии диалога
- Всем выбранным — одинаковое время
- Ручная корректировка: долгий тап + лог

### Отображение
- Две колонки: 2-й / 3-й класс
- Только `started` команды

---

## РАЗДЕЛ 9: КП И КАРТОЧКА

### Эталонный МЛ
- Настраивается в настройках соревнования
- Поля: `displayNumber`, `weight`, `type`, `sortOrder`, `normativeSeconds`, `class2/3`, `trackWait`, `trackExec`, `bonusPoints`

### Путевой лист
- Таблица: №, вес, тип, взят, ошибка, отсечка, штраф
- Подтверждение: секретарь + судья
- После двойного подтверждения → `read-only`

### Отсечка
- Время ожидания на тех. КП
- Вычитается из чистого времени
- `net_time = (finish - start) - Σ(offset_time)`

### Пересчёт `lost` → `finished`
Если чистое время ≤ КВ после учёта отсечек.

### CompetitionSettingsScreen

**Маршрут:** `competition_settings/{competitionId}`

**Назначение:** Просмотр и редактирование параметров соревнования, управление КП.

**Входные данные:** `competitionId: Long` (из аргументов навигации)

**ViewModel:** `CompetitionSettingsVM`

**Структура:**
1. TopAppBar — название (shortName → name fallback), «Назад», «Сохранить»
2. Блок «Контрольные пункты» — кластеры КП в рамках по классам, кнопка «Редактировать КП»
3. Блок «Параметры соревнования» — 4 числовых поля
4. Кнопка «СОХРАНИТЬ ВСЕ НАСТРОЙКИ»

**Особенности:**
- КП фильтруются по `forClass2` / `forClass3`
- Сортировка по `displayNumber`
- Клик по кластеру → диалог смены типа
- При пустом списке → подсказка + кнопка «Добавить КП»
- Автообновление КП при возврате с CheckpointListScreen (Flow)
- Сохранение в `settingsJson`: `{"start_interval":60,"control_time_2":90,"control_time_3":60,"min_team_size":2}`

**Навигация:**
- Вход: `CompetitionListScreen` (долгий тап или ⚙️)
- Выход 1: `CheckpointListScreen` (кнопка «Редактировать КП»)
- Выход 2: Назад → `CompetitionListScreen`

---

## РАЗДЕЛ 10: ИТОГИ

### Ранжирование
1. Статус: `finished` > `lost` > `disqualified`
2. Баллы: больше → выше
3. Чистое время: меньше → выше
### Призовые места
Всегда 3 (🥇🥈🥉). Под каждым может быть несколько команд.

### Деление мест
Судья вручную, с допуском по времени.

### Грамоты
PDF/HTML, ментор опционально, факсимиле (PNG).

### Текст для ведущего
Шаблон с подстановками `{gold_teams_list}` и т.д.

---

## РАЗДЕЛ 11: UI/UX

### Цвета
- `primary`: `#1E5A7A`
- `success`: `#00A86B`
- `warning`: `#FFC107`
- `danger`: `#D32F2F`
- `timer`: `#00C853` (контрастно-зелёный)

### Типографика
- Таймер: `Roboto Mono`, 40sp/24sp
- Заголовки: `Roboto Bold`, 24sp
- Основной: `Roboto Regular`, 16sp

### Размеры
- Кнопки: 56dp высота
- Плитки: 90dp высота, `aspectRatio 0.85`
- Статус-бар: 48dp

### Тёмная тема
Адаптивная, `primaryContainer` для инфоплитки.

---

## РАЗДЕЛ 12: ТЕХНИЧЕСКИЙ СТЕК

- Kotlin 1.9.22
- AGP 8.2.2, Gradle 8.5
- Compose BOM 2024.02.00
- Room 2.6.1, Hilt 2.48
- Navigation Compose 2.7.7
- Timber 5.0.1
- Security Crypto 1.1.0-alpha06

### Архитектура
MVVM + Clean Architecture:  `Entity` → `DAO` → `Repository` → `UseCase` → `ViewModel` → `Screen`

### Таймер
`TimerManager` (Singleton, Flow-based, без Service).  
Хранение `startTimestamp` в `app_settings`.  
Восстановление: `System.currentTimeMillis() - startTimestamp`.  
Резерв: `MIN(start_timestamp)` из `teams`.
<<<конец файла

docs/USER_GUIDE.md>>>
# 🏆 СпелеоСтарт: Инструкция судьи
*Коротко. По шагам. Без технических терминов.*

## 🔹 Быстрый старт (3 минуты)
1. Откройте приложение → «Соревнования» → «➕ Новое»
2. Введите название, дату, место → «СОЗДАТЬ»
3. Добавьте участников через «Персоны» → «➕ Новая персона»

### Экран «Настройки соревнования»

**Как открыть:** На экране списка соревнований нажмите и удерживайте карточку соревнования, или нажмите кнопку ⚙️.

**Блок «Контрольные пункты»:**
- КП отображаются круглыми кластерами: зелёные — обычные, оранжевые — технические
- Нажмите на кластер, чтобы изменить тип КП (обычный ↔ технический)
- Нажмите «Редактировать КП», чтобы перейти к полному редактированию списка

**Блок «Параметры соревнования»:**
- Интервал старта (секунды) — время между стартами команд
- Контрольное время 2-го класса (минуты)
- Контрольное время 3-го класса (минуты)
- Минимальный состав команды — минимальное число участников

**Сохранение:** Нажмите «💾 Сохранить» вверху или кнопку «СОХРАНИТЬ ВСЕ НАСТРОЙКИ» внизу. При ошибке валидации появится сообщение.


## 🔹 Зарегистрировать команду
1. Меню «Регистрация» → выберите класс
2. Введите фамилию:
   - ✅ Нашлось → выберите из списка
   - ❌ Нет → заполните прямо здесь:
     • Фамилия
     • Имя
     • Дата рождения (ДД.ММ.ГГГГ)
     > 💡 Поля «Отчество», «Телефон», «Позывной» скрыты. Нажмите «▸», чтобы заполнить сейчас, или сделайте это позже в полной карточке.
3. Если участнику <18 лет:
   - Приложение запросит ментора (≥18)
   - ✅ Нашлось → выберите
   - ❌ Нет → «➕ Быстрый ментор»: введите только Фамилию и Имя
   > 📌 Ментор привязывается к участнику. Если он уже в команде — просто выберите его.
4. Нажмите «СОХРАНИТЬ КОМАНДУ»

## 🔹 Начать старты
1. Меню «Старт» → «НАЧАТЬ СОРЕВНОВАНИЯ»
2. Очередь чередуется (2-й → 3-й класс) автоматически
3. Команда уходит по сигналу. Следующая появится сама

## 🔹 Зафиксировать финиш
1. Нажмите 🏁 в нижнем углу
2. Отметьте галочками финишировавшие команды
3. Нажмите «ПОДТВЕРДИТЬ»
> ⏱️ Время фиксируется в момент открытия окна. Если опоздали — долгий тап по времени → введите верно.

## 🔹 Заполнить путевой лист
1. Карточка команды → 📄 (или меню «Путевые»)
2. Для каждого КП: отметьте «Взят» / «Ошибка»
3. Для технических КП: укажите время ожидания
4. Введите пароль секретаря → «Карту заполнил»
5. Введите пароль судьи → «Карту проверил»
> 📌 После двойного подтверждения правки закрыты.
> ⚠️ Если в настройках отключено подтверждение: сохранение идёт сразу. Будьте внимательны.

## 🔹 Посмотреть итоги
1. Меню «Итоги» → выберите класс
2. Таблица: место, номер, баллы, время
3. Команды с пометкой `ОБГ` идут в конце списка. Их результаты считаются автоматически.

---

## ❓ Частые вопросы
| Вопрос | Ответ |
|--------|-------|
| Команда не стартовала вовремя | Нажмите «ПРОПУСК» → команда перейдёт в конец очереди |
| Ошиблись при вводе времени финиша | Долгий тап по времени → введите верно → сохраните |
| Участник выбыл | Карточка команды → «🔄 Заменить» |
| Приложение закрылось само | Откройте снова — таймер и данные восстановятся |
| Нужно экспортировать результаты | Главный экран → «📤 Экспорт сейчас» → файл в «Документы» |

## 🚫 Что нельзя делать
- Не удаляйте приложение до финализации соревнования
- Не меняйте время на устройстве во время старта
- Не подтверждайте путевой лист, пока не сверили отметки с бумажным носителем

## 🆘 Экстренная помощь
- Сброс таймера: долгий тап по таймеру → пароль `1234`
- Восстановление: «📥 Импорт» → выберите `autobackup.json`
<<<конец файла

docs/VALIDATION_RULES.md>>>
# 🛡️ VALIDATION_RULES.md · Правила валидации данных
**Дата:** 26.04.2026 | **Статус:** Рабочая | **Язык:** RU

``` markdown
## 1. ВВОД И ФОРМАТИРОВАНИЕ

| Поле | Правило | Где применяется |
|------|---------|-----------------|
| Фамилия, Имя, Отчество | 3 раздельных поля. **Title Case с явной русской локалью (`Locale("ru", "RU")`)**. Поддержка дефисных фамилий (Салтыков-Щедрин). Пробелы по краям обрезаются. | Регистрация персоны, инлайн-команда, быстрый ментор, редактирование |
| Дата рождения | Маска `ДД.ММ.ГГГГ`. Только цифры + авто-точки. Точная проверка возраста (с учётом дня/месяца). | Все формы ввода возраста |
| Телефон | Маска `+7 (XXX) XXX-XX-XX`. Валидация длины = 11 цифр. | Карточка персоны |
| Поиск персон | Мин. 3 символа фамилии ИЛИ 4 цифры телефона. Нормализация `Ё→Е`. | Автоподстановка в команде, менторе, замене |

**Технические детали Title Case:**
- Запрещено использовать `Locale.getDefault()` — на устройствах с английской локалью русские буквы обрабатываются некорректно
- Обязателен `Locale("ru", "RU")` для всех вызовов `uppercase()`/`lowercase()`
- Дефис (`-`) сбрасывает `capitalizeNext` для поддержки составных фамилий
- Пробел и точка также сбрасывают `capitalizeNext` (инициалы не ломаются)
```


## 2. ВОЗРАСТ И МЕНТОРЫ
| Возраст участника | Требование | Флаг в БД |
|-------------------|------------|-----------|
| ≥18 лет | Ментор не требуется | `mentorId = null` |
| 14–17 лет | Требуется ментор ≥18 + подтверждение | `mentorConfirmed = true` |
| <14 лет | Ментор ≥18 + подтверждение + разрешение судьи | `mentorConfirmed = true`, `judgeApproved = true` |

**Правила менторства:**
- 1 ментор → N подопечных в рамках одного соревнования.
- Ментор может входить в состав той же команды.
- Быстрая форма: только `Фамилия` + `Имя`. `canBeMentor=true` ставится автоматически. Дата рождения заполняется позже.
- В истории персоны: справка `[Соревнование] — ментор: [ФИО]`.

## 3. СОСТАВ КОМАНДЫ И ОБГ
- `min_team_size` читается из `competition.settings_json` (дефолт 2).
- Старт блокируется, если `участников < min_team_size`.
- Если `участников > min_team_size` → автоматическая метка `ОБГ`.
- ОБГ сортируется **после** стандартных зачётов в итогах. Ручная корректировка не требуется.

## 4. СТАТУСНЫЕ ПЕРЕХОДЫ (БЛОКИРОВКИ)
| Действие | Условие | Результат |
|----------|---------|-----------|
| Старт | `статус = registered` И `участников ≥ min` И `менторы подтверждены` | ✅ `started` |
| Финиш | `статус = started` | ✅ `finished` + фиксация времени |
| Расформирование | `registered` | ✅ без пароля → `free_agent` |
| Расформирование | `started` | ✅ пароль + причина → `registered` |
| Расформирование | `finished / lost / disqualified` | ❌ Запрещено |
| Смена класса | `started ≤5 мин` | ✅ пароль + штраф |
| Смена класса | `started >5 мин / finished` | ❌ Только откат/дисквалификация |

## 5. ПУТЕВОЙ ЛИСТ И ПОДТВЕРЖДЕНИЕ
- **Настройки соревнования:**
  - `Старший судья` (Да/Нет) → «Нет» только после пароля разработчика.
  - `Секретарь` (Да/Нет) → отключение только после пароля судьи.
- **Поведение:**
  - Если любой флаг `Нет` → показывать `⚠️ Сохраняется без подтверждений — будьте внимательны`.
  - Если оба `Да` → обязательный двойной ввод паролей. После → `checkpointsEntered = true`, режим `read-only`.
  - `Взят + Ошибка` → вес = 0, отсечка учитывается в чистом времени.
  - `Не взят` → вес = 0, отсечка не учитывается.
  - Автопересчёт: если `статус = lost` И `чистое_время ≤ КВ` → `статус = finished`.


### CompetitionSettingsScreen — валидация полей

| Поле | Правило | Сообщение об ошибке |
|------|---------|---------------------|
| Интервал старта | > 0 | «Интервал старта должен быть положительным числом» |
| Контрольное время 2-го класса | > 0 | «КВ для 2-го класса должно быть положительным числом» |
| Контрольное время 3-го класса | > 0 | «КВ для 3-го класса должно быть положительным числом» |
| Минимальный состав команды | ≥ 1 | «Минимальный состав команды должен быть не менее 1» |

**Формат сохранения:** JSON в поле `competition.settingsJson`
```json
{
    "start_interval": 60,
    "control_time_2": 90,
    "control_time_3": 60,
    "min_team_size": 2
}
```


## 6. ТЕХНИЧЕСКИЕ ОГРАНИЧЕНИЯ
| Область | Правило |
|---------|---------|
| БД | `version++` строго при изменении любого `@Entity`. Порядок очистки: дети → родители. |
| ID | Запрещён хардкод `1L`. Передача только через `SharedState` или `navArgument`. |
| Insert | Всегда проверять `insert() != -1L`. При `-1L` → лог + возврат, без продолжения транзакции. |
| UI | Мин. тап 48dp. Цвета только через `MaterialTheme.colorScheme` или явную проверку светлой/тёмной темы. |
| Таймер | Фиксация финиша при открытии диалога. Восстановление: `System.currentTimeMillis() - startTimestamp` из `app_settings`. |

## 7. ЧЕК-ЛИСТ ПЕРЕД РЕЛИЗОМ ЭКРАНА
- [ ] Все поля ввода проверяются на пустоту/формат до вызова `suspend fun`
- [ ] Возрастные флаги (`mentorConfirmed`, `judgeApproved`) обновляются реактивно
- [ ] `min_team_size` берётся из `settings_json`, не захардкожен
- [ ] Нет `Log.e` в release-сборке (использовать `Timber`)
- [ ] Нет `Color(0xFF...)` без проверки темы
- [ ] Версия БД соответствует схеме `AppDatabase.kt`
- [ ] Тест на чистой БД: `uninstall` → `clean` → `run` → генерация → старт → финиш → итоги
<<<конец файла

docs/HTML/Screen_competition.html>>>
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>КП по классам — SpeleoStart</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #121212;
            font-family: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Карточка настроек */
        .settings-card {
            max-width: 500px;
            width: 100%;
            background: #1e1e1e;
            border-radius: 20px;
            padding: 20px;
            box-shadow: 0 8px 24px rgba(0,0,0,0.3);
        }

        /* Заголовок карточки */
        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            margin-bottom: 20px;
            padding-bottom: 12px;
            border-bottom: 1px solid #333;
        }

        .card-header h2 {
            color: #e0e0e0;
            font-size: 18px;
            font-weight: 500;
        }

        .card-header span {
            color: #4CAF50;
            font-size: 13px;
            background: #2c2c2c;
            padding: 4px 10px;
            border-radius: 20px;
        }

        /* Блок с КП */
        .checkpoints-section {
            margin-bottom: 24px;
        }

        .section-title {
            color: #aaa;
            font-size: 12px;
            letter-spacing: 0.5px;
            margin-bottom: 12px;
        }

        /* Строка класса */
        .class-row {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 12px;
            background: #252525;
            border-radius: 12px;
            padding: 6px 12px;
            min-height: 48px;
        }

        .class-row:last-child {
            margin-bottom: 0;
        }

        /* Лейбл класса */
        .class-label {
            width: 65px;
            font-size: 13px;
            font-weight: 600;
            color: #bbb;
            letter-spacing: 0.5px;
        }

        /* Контейнер кластеров */
        .cluster-list {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            flex: 1;
        }

        /* Кластер (кружок с номером) */
        .cluster {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            font-size: 14px;
            font-weight: 500;
            transition: transform 0.1s ease;
            cursor: default;
        }

        /* Обычный КП — зелёный (приглушённый) */
        .cluster.normal {
            background: #2E7D32;
            color: white;
        }

        /* Технический КП — оранжевый */
        .cluster.technical {
            background: #F57C00;
            color: white;
        }

        /* КП, не входящие в класс — скрыты */
        .cluster.hidden {
            display: none;
        }

        /* Разделитель полей настроек (для контекста) */
        .settings-divider {
            height: 1px;
            background: #333;
            margin: 20px 0 16px 0;
        }

        .setting-field {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }

        .setting-field label {
            color: #ccc;
            font-size: 14px;
        }

        .setting-field .value {
            color: #4CAF50;
            background: #2c2c2c;
            padding: 6px 12px;
            border-radius: 8px;
            font-size: 14px;
        }

        .save-btn {
            width: 100%;
            background: #0D47A1;
            color: white;
            border: none;
            padding: 12px;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 500;
            margin-top: 8px;
            cursor: pointer;
        }

        .save-btn:hover {
            background: #1565C0;
        }

        .note {
            color: #666;
            font-size: 11px;
            text-align: center;
            margin-top: 16px;
        }
    </style>
</head>
<body>
<div class="settings-card">
    <div class="card-header">
        <h2>⚙️ Бяковские каменоломни 2026</h2>
        <span>Активно</span>
    </div>

    <!-- БЛОК: Контрольные пункты -->
    <div class="checkpoints-section">
        <div class="section-title">🗺️ КОНТРОЛЬНЫЕ ПУНКТЫ</div>

        <!-- КЛАСС 2 -->
        <div class="class-row">
            <div class="class-label">КЛАСС 2</div>
            <div class="cluster-list" id="class2List"></div>
        </div>

        <!-- КЛАСС 3 -->
        <div class="class-row">
            <div class="class-label">КЛАСС 3</div>
            <div class="cluster-list" id="class3List"></div>
        </div>
    </div>

    <div class="settings-divider"></div>

    <!-- Прочие настройки (для контекста) -->
    <div class="setting-field">
        <label>📏 Интервал старта</label>
        <div class="value">60 сек</div>
    </div>
    <div class="setting-field">
        <label>⏱️ КВ 2-й класс</label>
        <div class="value">90 мин</div>
    </div>
    <div class="setting-field">
        <label>⏱️ КВ 3-й класс</label>
        <div class="value">60 мин</div>
    </div>
    <div class="setting-field">
        <label>👥 Мин. состав команды</label>
        <div class="value">2 чел</div>
    </div>

    <button class="save-btn" onclick="alert('Настройки сохранены (демо)')">💾 СОХРАНИТЬ НАСТРОЙКИ</button>
    <div class="note">КП: зелёные — обычные, оранжевые — технические</div>
</div>

<script>
    // Моковые данные КП (MasterRouteCardEntity)
    const checkpoints = [
        { displayNumber: 1, type: "normal", forClass2: true, forClass3: true },
        { displayNumber: 2, type: "normal", forClass2: true, forClass3: true },
        { displayNumber: 3, type: "technical", forClass2: true, forClass3: true },
        { displayNumber: 4, type: "normal", forClass2: true, forClass3: false },
        { displayNumber: 5, type: "normal", forClass2: true, forClass3: true },
        { displayNumber: 6, type: "technical", forClass2: true, forClass3: true },
        { displayNumber: 7, type: "normal", forClass2: true, forClass3: false },
        { displayNumber: 8, type: "normal", forClass2: true, forClass3: true },
        { displayNumber: 9, type: "normal", forClass2: false, forClass3: true },
        { displayNumber: 10, type: "technical", forClass2: false, forClass3: true },
        { displayNumber: 11, type: "normal", forClass2: false, forClass3: true },
        { displayNumber: 12, type: "normal", forClass2: true, forClass3: false },
        { displayNumber: 13, type: "normal", forClass2: true, forClass3: false },
        { displayNumber: 14, type: "technical", forClass2: true, forClass3: true }
    ];

    function renderClusters() {
        const class2Container = document.getElementById('class2List');
        const class3Container = document.getElementById('class3List');

        class2Container.innerHTML = '';
        class3Container.innerHTML = '';

        checkpoints.forEach(cp => {
            // КЛАСС 2
            if (cp.forClass2) {
                const cluster = document.createElement('div');
                cluster.className = `cluster ${cp.type}`;
                cluster.textContent = cp.displayNumber;
                class2Container.appendChild(cluster);
            }

            // КЛАСС 3
            if (cp.forClass3) {
                const cluster = document.createElement('div');
                cluster.className = `cluster ${cp.type}`;
                cluster.textContent = cp.displayNumber;
                class3Container.appendChild(cluster);
            }
        });
    }

    renderClusters();
</script>
</body>
</html>
<<<конец файла

