# 📘 SPEC_MASTER.md · SpeleoStart v4.5
**Дата:** 11.05.2026 | **Статус:** Актуально

## 1. ГЛОССАРИЙ

| Термин | Значение |
|--------|----------|
| `Competition` | Мероприятие. Цикл: СОЗДАНО → АКТИВНО → ЗАВЕРШЕНО → АРХИВ → УДАЛЕНО |
| `Team` | Команда. Статусы: `registered → started → finished / lost / disqualified` |
| `Checkpoint (КП)` | Точка. Типы: `normal` (вес), `technical` (вес + отсечка + штраф) |
| `КВ` | Контрольное время. 2 кл = 90 мин, 3 кл = 60 мин |
| `Ментор` | Лицо ≥18 лет. Подтверждает ответственность за участника <18 |
| `Мастер` | Пользователь с паролем `devdebug`. Имеет права редактирования без потери статусов |

## 2. ОТОБРАЖЕНИЕ ВРЕМЕНИ

| Сценарий | Отображение |
|----------|-------------|
| Соревнование не запущено | `—:—:—` |
| Команда не стартовала | `—:—:—` |
| Команда стартовала | `+ЧЧ:ММ:СС` (относительно старта соревнования) |
| Команда финишировала | `+ЧЧ:ММ:СС` (относительно старта соревнования) |

**Формула:** `относительное_время = (timestamp_команды - timestamp_старта_соревнования) / 1000`

## 3. ЦВЕТОВЫЕ МЕТКИ КОМАНД

| Метка | Условие | Цвет |
|-------|---------|------|
| `18+` | Все ≥18 | `#00A86B` |
| `14+` | Есть 14-17, `mentorConfirmed = true` | `#FFC107` |
| `<14ок` | Есть <14, ментор + `judgeApproved = true` | `#FFC107` |
| `<17 !!!` | Есть 14-17, ментор НЕ подтверждён | `#FF8C00` |
| `<14 !!!` | Есть <14, НЕТ разрешения судьи | `#FF8C00` |
| `СНЯТЫ` | `disqualified` | `#D32F2F` |

## 4. СТАТУСЫ ПУТЕВОГО ЛИСТА

| Статус | Отображение | Цвет | Действия |
|--------|-------------|------|----------|
| CONFIRMED | ✅ ПУТЕВОЙ ЛИСТ | Зелёный | 🔓 Мастер-правка |
| IN_PROGRESS | 📝 ПУТЕВОЙ ЛИСТ | Оранжевый | 📝 ЗАПОЛНИТЬ |
| EMPTY | 📋 ПУТЕВОЙ ЛИСТ | Серый | 📝 ЗАПОЛНИТЬ |
| CANCELLED | ❌ ПУТЕВОЙ ЛИСТ | Красный | Нет действий |
| MASTER_MODE | 👑 МАСТЕР-РЕЖИМ | Оранжевый | 💾 СОХРАНИТЬ |

## 5. МАСТЕР-ПРАВКА (пароль `devdebug`)

**ВАЖНО:** Мастер-правка **реально сохраняет изменения в БД**. После сохранения и повторного входа в карточку команды изменения отображаются корректно.

**Алгоритм работы:**
1. Нажать кнопку "🔓 Мастер-правка" в карточке команды
2. Ввести пароль `devdebug`
3. Внести изменения в путевой лист (кластеры кликабельны)
4. Нажать "💾 СОХРАНИТЬ" в верхней панели
5. Статусы "подписано" (секретарь/судья) сохраняются
6. Данные записываются в БД и больше не сбрасываются

## 5.1 АВТОМАТИЧЕСКОЕ ОТКРЫТИЕ РЕДАКТИРОВАНИЯ ПУТЕВОГО ЛИСТА

**Правило:** При любом переходе в карточку команды со статусом `finished` и флагом `checkpointsEntered = false` автоматически включается режим быстрого редактирования КП.

**Алгоритм:**
1. Загружается `TeamCardScreen` с `teamId`
2. В `LaunchedEffect(uiState.teamInfo, uiState.mode)` проверяются условия:
  - `team.status == "finished"`
  - `!team.checkpointsEntered`
  - `uiState.mode == TeamCardMode.VIEW`
  - Не мастер-режим и не режим редактирования
3. При выполнении условий вызывается `vm.enterQuickEditMode()`
4. Пользователь сразу видит кликабельные кластеры КП и кнопки подписей

**Точки входа, где применяется авто-редактирование:**
- Из диалога «Путевые» на главном экране
- Из списка команд (`TeamListScreen`)
- Из поиска команд
- Из любого другого места, ведущего в карточку finished команды


## 6. ТЕХНИЧЕСКИЕ КП (ОТСЕЧКИ И ШТРАФЫ)

### 6.1 Формат ввода отсечки
- **Формат:** `ММ:СС` (минуты:секунды)
- **Допустимые значения:** минуты `00-59`, секунды `00-59`
- **Пустое поле** = нет отсечки
- **Ошибки валидации:**
    - `"12"` → ❌ невалидно (нужно `12:00` или `00:12`)
    - `"01:99"` → ❌ невалидно (секунды не могут быть 99)
    - `"99:99"` → ❌ невалидно (оба значения за пределами)

### 6.2 Валидация в диалоге
- **Кнопка "СОХРАНИТЬ" активна ТОЛЬКО** когда данные корректны
- При ошибке под полем ввода отображается **красная подсказка**
- **Placeholder:** `мм:сс` (серый, полупрозрачный) — не выглядит как введённое значение
- **Supporting text:** `"Пример: 01:30 (пустое поле = без отсечки)"`

### 6.3 Формат ввода штрафа
- **Формат:** целое число (положительное = бонус, отрицательное = штраф)
- **Допустимые значения:** от `-999` до `999`
- **Пустое поле** = 0 штрафа
- **Ошибки валидации:**
    - `"abc"` → ❌ невалидно
    - `"1000"` → ❌ невалидно (выход за пределы)

### 6.4 Визуальное оформление поля отсечки
| Элемент | Оформление |
|---------|------------|
| Шрифт | Моноширинный (`FontFamily.Monospace`) |
| Placeholder | `мм:сс`, 14.sp, `Color.Gray.copy(alpha = 0.4f)` |
| Label | `Отсечка (ММ:СС)`, 14.sp |
| Supporting text | 10.sp, серый (подсказка формата) |
| Ошибка | Красный текст, 11.sp, блокировка кнопки "СОХРАНИТЬ" |

## 7. ПОДПИСАНИЕ ПУТЕВОГО ЛИСТА

**Важно:** При подписании секретарём или судьёй:
1. Сначала **сохраняются все текущие изменения** в БД
2. Затем проставляются подписи
3. Затем проверяется возможность закрытия ПЛ
4. Затем перезагружаются данные (включая `checkpointsEntered`)

Это гарантирует, что ни один введённый КП не будет потерян.

## 8. ТЕХНИЧЕСКИЕ ОГРАНИЧЕНИЯ

- **БД:** Room 2.6+, версия `8`
- **ID:** Никогда не хардкодить. Передача через `SharedState` или `navArgument`
- **Очистка БД:** дети → родители
- **Курсор в автоформатировании:** пересчёт через количество значащих символов
- **Title Case:** явная русская локаль `Locale("ru", "RU")`
- **Время старта соревнования:** хранится в `AppSettingsDao` по ключу `competition_start_timestamp`

## 9. ПАРОЛИ

| Роль | Пароль |
|------|--------|
| Секретарь | `secret123` |
| Судья | `judge123` или `1234` |
| Мастер/Админ | `devdebug` |

## 10. NEVER-DO

1. Не повышать версию БД без изменения Entity
2. Не хардкодить ID
3. Не удалять родителей до детей
4. Не использовать `Locale.getDefault()` для русского языка
5. Не использовать `selection.start.coerceIn()` при автоформатировании
6. Не сбрасывать статусы "подписано" при мастер-правке
7. **Не допускать тихого отбрасывания некорректных данных** — всегда явная ошибка с блокировкой сохранения


## 11. ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### 11.1 Мастер-режим редактирования КП (v4.6)

**Статус:** ❌ НЕ РАБОТАЕТ

**Симптомы:**
- Кластеры КП не меняют состояние при нажатии (только виброотклик)
- Меню отсечек для технических КП не открывается
- Кнопка "Сохранить" отсутствует или неактивна

**Требуется:** Полная ревизия `TeamCardVM` и `TeamCardComponents.kt`